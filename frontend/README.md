# Frontend (React + Vite)

## Run

```bash
cd frontend
npm install
npm run dev
```

By default the UI calls `http://localhost:8000`.
Override with:

```bash
set VITE_API_BASE=http://localhost:8000
npm run dev
```
